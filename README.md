# anti-dds

A simple and fast script to block **China (CN)** and **Russia (RU)** IP ranges on Ubuntu or Debian servers.

## How to run

Run the following command on your server (as root):

```bash
bash <(curl -Ls --ipv4 https://raw.githubusercontent.com/mgod122/anti-dds/main/geoblock-cn-ru.sh)
